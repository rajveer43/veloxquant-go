package optimize

// AutoPilot, VeloxQuant's end-to-end hardware-aware model and
// optimization-plan selection, is implemented at the top level (see
// autopilot.go in the root package) because it composes the system,
// memory, models, and optimize packages together. This file is reserved
// for optimize-specific autopilot heuristics as they're added.
